#include <iostream>
using namespace std; // With this line included you don't have to use std inside the main function

int main(){
    int x = 10;
    double y = 5;
    double z = (x + 10)/ (3*y);

    cout << z;
}